\begin{frame}{Схема исследования}
	\begin{center}
		\resizebox{1.025\textwidth}{!}{
			\begin{tikzpicture}[
				node distance=0.6cm, % Увеличен зазор между соседними блоками
				param/.style={rectangle, rounded corners, draw=Burgundy!50, fill=Burgundy!1!Ivory, thick, text width=3.6cm, align=center, font=\large, text=DeepBlue, minimum height=3cm},
				%
				opt/.style={rectangle, rounded corners, draw=Burgundy!80, fill=Burgundy!5, thick, text width=4.0cm, align=center, font=\large, text=DeepBlue, minimum height=4.5cm},
				%
				solver/.style={rectangle, draw=DeepBlue!60, fill=DeepBlue!5, thick, rounded corners, text width=6.5cm, align=center, font=\large, minimum height=2.2cm, text=DeepBlue},
				%
				fitness/.style={rectangle, draw=DeepBlue!60, fill=DeepBlue!5, thick, rounded corners, text width=4.4cm, align=center, font=\large, text=DeepBlue, minimum height=2.5cm},
				%
				note_box/.style={
					rectangle,
					rounded corners,
					draw=DeepBlue!40,
					fill=DeepBlue!1!Ivory,
					thick,
					text width=10cm,
					align=left,
					font=\Large,
					text=DeepBlue,
					minimum height=4cm,
					inner xsep=14pt,
					inner ysep=10pt
				},
				%
				arrow/.style={-Stealth,  thick, DeepBlue!80}
				]
				
				% --- ЦЕНТРАЛЬНЫЕ БЛОКИ ---
				
				% Входные параметры
				\node (start) [param] {
					\textbf{Входные данные} \\
					\vspace{6pt}
					\raggedright
					\begin{itemize}
						\item задача
						\item $\theta \in [0, 1]^4$
					\end{itemize}
				};
				
				% Оптимизатор
				\node (algo) [opt, right=of start] {
					\textbf{Оптимизация} \\
					\vspace{5pt}
					$\mathcal{F}(\theta) \rightarrow \min$ \\
					\vspace{8pt}
					Сеточный поиск \\
					$\downarrow$ \\
					Рой частиц (PSO) \\
					$\downarrow$ \\
					Нелдер--Мид
				};
				
				% --- ВЕРТИКАЛЬНЫЙ СТЕК ЗАДАЧ КОШИ ---
				
				% Задача 2
				\node (t2) [solver, right=1.2cm of algo, yshift=1.5cm]{
					\textbf{2. Уравнение Шрёдингера} \\  ($h = 0.05, \; T \le 2$)
				};
				
				% Задача 1
				\node (t1) [solver, above=0.2cm of t2] {
					\textbf{1. Гармонический осциллятор} \\ ($h = 0.1, \; T \le 10$)
				};
				
				% Задача 3
				\node (t3) [solver, right=1cm of algo, yshift=-1.5cm] {
					\textbf{3. Задача двух тел} \\  ($h = 0.1, \; T \le 15$)
				};
				
				% Задача 4
				\node (t4) [solver, below=0.2cm of t3]{
					\textbf{4. Задача пяти внешних тел} \\  ($h = 0.2, \; T \le 20$)
				};
				
				% --- ПОСЛЕДУЮЩИЕ БЛОКИ ---
				
				% Оценка качества (Сдвиг -2.0cm идеально возвращает блок на центральную ось схемы)
				\node (eval) [fitness, right=1.2cm of t2, yshift=-1.5cm] {
					\textbf{Критерий качества} \\
					\vspace{6pt}
					Вычисление $\mathcal{F}(\theta)$
				};
				
				% Выходные данные
				\node (stop) [param, right=of eval] {
					\textbf{Выходные данные} \\
					\vspace{6pt}
					$\theta^*$
				};
				
				\node (info_note)
				[note_box,
				anchor=south,
				at={(t1.north)},
				yshift=1.2cm,
				text width=18cm]
				{
					MacBook Pro (2019), Intel Core i7 (6 ядер, 2.6 ГГц), 	однопоточные вычисления \\
					• Оценка $\mathcal{F}(\theta)$ на задачу: 900 раз \\
					• Итераций алгоритма PSO: 30 \\
					• Число частиц в рое: 30
				};
				
				% --- СВЯЗИ И СТРЕЛКИ ---
				
				\draw [arrow] (start) -- (algo);
				\draw [arrow] (eval) -- (stop); 
				
				% Разветвление стрелок от Оптимизатора к Задачам (вынос увеличен до 0.4 для простора)
				\draw [arrow] (algo.east) -- ++(0.4,0) |- (t1.west);
				\draw [arrow] (algo.east) -- ++(0.4,0) |- (t2.west);
				\draw [arrow] (algo.east) -- ++(0.4,0) |- (t3.west);
				\draw [arrow] (algo.east) -- ++(0.4,0) |- (t4.west);
				
				% Схождение стрелок от Задач к Критерию качества
				\coordinate (busR) at ($(eval.west)+(-1.0,0)$);
				
				\draw [arrow] (t1.east) -- (busR |- t1.east) |- (eval.west);
				\draw [arrow] (t2.east) -- (busR |- t2.east) |- (eval.west);
				\draw [arrow] (t3.east) -- (busR |- t3.east) |- (eval.west);
				\draw [arrow] (t4.east) -- (busR |- t4.east) |- (eval.west);
				
				% Нижняя петля обратной связи (Зазор опущен до -5.6cm, чтобы гарантированно обойти массивный нижний край блока t4)
				\draw [arrow] (eval.south) -- ++(0,-4.25) -| (algo.south);
				
			\end{tikzpicture}
		}
	\end{center}
\end{frame}